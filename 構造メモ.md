様々な事情により運勢から引いたタスクを決めるのではなく、引いたタスクをもとに運勢を決めることにします
つまり抽選されるのは実質運勢ではなくタスクです
乱数など不確実性をもって決められるなら過程がどうであれ運勢は運勢...？

↑Discordの発言
先輩方が作ったシステムを大規模にぶち壊して改修します
改修するにあたって何がどう動いているか言語化しないと修正時に予期しない不具合が生じるので、ここにまとめます

1.main.tsでdrawButtonの押下を検知
2.omikuji.tsのdrawOmikuji()とrender.tsのrenderResultを呼び出し
3.omikuji.tsのomikujiRatiosの割合に従っておみくじの結果を返す
4.main.tsで呼び出したrenderResultにdrawOmikujiの結果を入れて終了

↓は一応作った乱数
処理の対象を逆転するのである程度使えるはず
export function drawtask(): string {
const brank = gettaskValues().includes("");
if (gettaskValues().length == 1 && brank) {
console.log("タスク無し！やることが無いってのも寂しくない？");
}
//タスクリストから空欄を消して再度リスト出力

const TruetaskValues = gettaskValues().filter((value) => {
return value != "";
});
//重み付き抽選の実装
//なんか運勢が決まったら
//1.大吉を始点としてその運勢の順番を調べる
//2.その順番から若い順番の「割合」を全部足したものをj,その順番だけを足してないものをiとする
//例えば「小吉」の場合(i,j)=(0.2,0.4)、大吉の場合(0,0.05)となる
//3.大吉～凶の「割合」を合計してsumに代入
//4.i = i*TruetaskValues.length/sum,j=j*TruetaskValues.length/sumを行う
//5.地域i~jで乱数生成、そのintをとり、その番号でタスクを選ぶ

//1.2.を行う
const LuckNumber = Object.keys(omikujiRatios).indexOf(omikujiState.result!); //indexOfでの運勢の順番を調べる
let i = 0;
let j = 0;
let LuckRatioSum = 0;
//console.log("出た運勢の番号", LuckNumber);
for (let n: number = LuckNumber; n >= 0; n--) {
// console.log("n=", n);
const pick*luck = Object.entries(omikujiRatios); //下記のようにpick_luck[a][b]で中身が見れるようにする
//console.log("pick_luck", pick_luck[n][1]);
j = j + pick_luck[n][1];
}
for (let n: number = LuckNumber - 1; n >= 0; n--) {
// console.log("n=", n);
const pick_luck = Object.entries(omikujiRatios); //下記のようにpick_luck[a][b]で中身が見れるようにする
// console.log("pick_luck", pick_luck[n][1]);
i = i + pick_luck[n][1];
}
//3.を行う
for (let n: number = pick_luck.length - 1; n >= 0; n--) {
LuckRatioSum = LuckRatioSum + pick_luck[n][1];
}
//4.を行う
i = (i * TruetaskValues.length) / LuckRatioSum;
j = (j \_ TruetaskValues.length) / LuckRatioSum;
//console.log("地域i,j", i, j);

// console.log("pick*luck.length", pick_luck.length);
// console.log("sum", LuckRatioSum);
//5.を行う　floorは少数切り捨ての意
const randomIdxtask = Math.floor(Math.random() * (j - i) + i);
const drawntask = TruetaskValues[randomIdxtask];
console.log(drawntask); //randomIdxtask, Math.random() \_ TruetaskValues.length);
// console.log("タスクリスト", gettaskValues());
// console.log("タスクリスト長さ", gettaskValues().length);
// console.log("空欄抜きtask", TruetaskValues);
// console.log("空欄抜きtask長さ", TruetaskValues.length);
//console.log("i=", i, "j=", j, "random=", randomIdxtask);
return drawntask;
}

タスク選択周り
タスク追加欄で入力する空欄混じりのタスク列で、
空欄を抜いて取得したタスクの順番（Truetaskあたり）
空欄を抜かずに取得したタスクの順番
の二つが存在する。
前者は選択したタスクが全タスクでどれくらいの位置にあるかを調べ、どの運勢を選ぶかに使う。
後者は前者のタスクの順番と、完了ボタンの処理を結びつけることに使う。